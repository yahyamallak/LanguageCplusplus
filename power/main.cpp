#include <iostream>
#include <cmath>

using namespace std;

int main(){

    int a, n;

    cout << "Enter a number : ";
    cin >> a;

    cout << "Enter a power : ";
    cin >> n;

    cout << "A power N : " << pow(a,n);

    return 0;
}